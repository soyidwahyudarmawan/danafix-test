spring:
  application:
    name: alami
  datasource:
    url: jdbc:postgresql://localhost:5432/danafix-test
    username: postgres
    password: root

server:
  port: 8282
  servlet:
    context-path: /danafix
